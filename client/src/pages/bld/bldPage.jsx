import React from "react";

import SessionConfig from "../../components/session-config/session-config.component";

const BldPage = () => {
  return <SessionConfig title="BLD SESSION" room="bld" />;
};

export default BldPage;
